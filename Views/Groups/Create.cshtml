@model Group
@{
    ViewData["Title"] = "Create Group";
}
<link rel="stylesheet" href="~/css/groups.css" />

<div class="card">
    <div class="card-header">
        <h2><i class="fas fa-plus-circle"></i> Create New Group</h2>
    </div>

    <form asp-action="Create" method="post">
        <div class="form-group">
            <label asp-for="Name">Group Name</label>
            <input asp-for="Name" class="form-control" placeholder="e.g., Adult - Male, Young Adult - Female" required />
            <span asp-validation-for="Name" class="validation-error"></span>
        </div>

        <div class="form-group">
            <label asp-for="Type">Group Type</label>
            <select asp-for="Type" class="form-control" required>
                <option value="">-- Select Type --</option>
                <option value="@GroupType.Adult">Adult</option>
                <option value="@GroupType.YoungAdult">Young Adult</option>
                <option value="@GroupType.Children">Children</option>
            </select>
            <span asp-validation-for="Type" class="validation-error"></span>
        </div>

        <div class="form-group">
            <label asp-for="GenderRestriction">Gender Restriction (Optional)</label>
            <select asp-for="GenderRestriction" class="form-control">
                <option value="">-- Any Gender --</option>
                <option value="@Gender.Male">Male Only</option>
                <option value="@Gender.Female">Female Only</option>
            </select>
        </div>

        <div class="form-group">
            <label asp-for="Description">Description</label>
            <textarea asp-for="Description" class="form-control" rows="3" placeholder="Optional description"></textarea>
        </div>

        <div class="d-flex gap-2">
            <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> Create Group</button>
            <a href="@Url.Action("Index", "Groups")" class="btn cancel-btn"><i class="fas fa-times"></i> Cancel</a>
        </div>
    </form>
</div>

